master:
    script:
        - mvn source:jar deploy -B -DskipTests
    tags:
        - mvn
    only:
        - master
branches:
    script:
        - mvn test -U -B
    tags:
        - mvn
    only:
        - branches
    except:
        - master
